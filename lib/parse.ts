export function parseResume(fileBuffer: Buffer) {
  // Lógica para extraer texto de un PDF e identificar datos clave.
  return {
    name: 'Candidato Demo',
    skills: ['JavaScript', 'React']
  }
}
